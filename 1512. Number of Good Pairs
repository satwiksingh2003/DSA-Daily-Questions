class Solution {
    public int numIdenticalPairs(int[] nums) {
        int[] freq = new int[101]; // constraints: nums[i] <= 100
        int count = 0;
        for (int num : nums) {
            count += freq[num]; // already seen numbers form pairs
            freq[num]++;
        }
        return count;
    }
}
