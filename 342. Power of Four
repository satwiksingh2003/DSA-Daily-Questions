class Solution {
    public boolean isPowerOfFour(int n) {
        // n must be positive
        if (n <= 0) return false;
        // check power of 2 AND only one bit in correct position for power of 4
        return (n & (n - 1)) == 0 && (n & 0x55555555) != 0;
    }
}
