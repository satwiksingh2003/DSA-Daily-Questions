class Solution {
    public double angleClock(int hour, int minutes) {
        double angle = Math.abs((hour*30)-(minutes*11.0/2));
        if(angle<=180){
            return angle;
        }
        return(360-angle);
    }
}
